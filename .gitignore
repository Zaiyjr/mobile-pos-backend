node_modules
# Keep environment variables out of version control
.env
.env.local

.vercel
.vercel-tsc
dist/
api/**/*.js
api/**/*.d.ts
api/**/*.map
src/**/*.js
src/**/*.map
# Hand-written .d.ts under src/ are SOURCE and must stay tracked
# (e.g. shared/domain/types/express.d.ts); compiled declarations emit to dist/.
*.tsbuildinfo
